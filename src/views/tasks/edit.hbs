<div class="row">
  <div class="col-md-4 offset-md-4">
    <div class="card">
      <div class="card-body"></div>
        <form action="/tasks/edit/{{task._id}}"  method="POST">
          <div class="for-group">
            <input type="text" name="titulo" value="{{task.titulo}}" placeholder="write your titulo"
            class="form-control">
          </div>
          <div class="form-group">
            <input class="form-control" value="{{task.descripcion}}" type="text" name="descripcion" placeholder="write a descripcion">
          </div>
          <button type="submit" class="btn btn-succes btn-block">
            Save
          </button>
        </form>
      </div>
    </div>
</div>
